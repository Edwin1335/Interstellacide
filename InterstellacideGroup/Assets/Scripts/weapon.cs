using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Weapon : MonoBehaviour
{
    // public Transform fire; //point of fire from gun
    // public int damage = 10; //damage enemy takes 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // if(Input.GetButtonDown("shoot")){

        //     //shoot ray and store data about what we hit
        //     RaycastHit2D hit = Physics2d.RayCast(fire.precision, firepoint.right);

        //     if(hit){
        //         Enemy enemyHit = hit.transform.GetComponent<Enemy>();
        //         //if we hit enemy make sure he takes damage
        //         if(enemyHit != null){
        //             // enemyHit.takeDamage(damage);
        //         }


        //         //@TODO: add impact effect 

        //         //@TODO: add ray cast line to see ray when fired

        //     }

        // }
        
    }
}
